# DeepSeek 余额监控 - UI 设计稿

## 整体风格
- Material3 Design，支持动态取色（Android 12+）和深色/浅色主题
- 主色调：#4A90D9（科技蓝）
- 卡片圆角：12dp，阴影：2-4dp

## 页面一：主仪表盘 (Dashboard)

```
┌──────────────────────────────────────┐
│ ● DeepSeek 余额监控           [🔄][⚙] │  ← TopAppBar, 主色背景
├──────────────────────────────────────┤
│                                      │
│ ┌──────────────────────────────────┐ │
│ │ 💰 账户余额                      │ │  ← 主色容器卡片
│ │                                  │ │
│ │     ¥ 85.2340                    │ │  ← 36sp 加粗
│ │                                  │ │
│ │  赠金余额        充值余额        │ │  ← label
│ │  ¥ 50.00         ¥ 35.23        │ │  ← 18sp
│ └──────────────────────────────────┘ │
│                                      │
│ ┌──────────────┐ ┌──────────────┐   │
│ │   今日消耗     │ │   本月消耗    │   │  ← surfaceVariant 卡片
│ │              │ │              │   │
│ │   ¥ 1.2340   │ │   ¥ 45.6700  │   │  ← 22sp 加粗
│ └──────────────┘ └──────────────┘   │
│                                      │
│ ┌──────────────┐ ┌──────────────┐   │
│ │   V4 Flash   │ │    V4 Pro    │   │  ← secondaryContainer 卡片
│ │              │ │              │   │
│ │   128.5K     │ │    45.2K     │   │  ← 20sp 加粗
│ │   tokens     │ │    tokens    │   │  ← label
│ └──────────────┘ └──────────────┘   │
│                                      │
│ ┌──────────────────────────────────┐ │
│ │ 每日Token消耗                    │ │  ← 标题
│ │                                  │ │
│ │  ▓                               │ │
│ │  ▓ ▓         ▓                   │ │  ← 蓝色柱状图
│ │  ▓ ▓ ▓     ▓ ▓                   │ │
│ │  ▓ ▓ ▓ ▓ ▓ ▓ ▓ ▓               │ │
│ │  06-01  06-03  06-05  06-07     │ │  ← 日期标签
│ └──────────────────────────────────┘ │
│                                      │
└──────────────────────────────────────┘
```

## 页面二：设置 (Settings)

```
┌──────────────────────────────────────┐
│ [←] 设置                             │  ← TopAppBar
├──────────────────────────────────────┤
│                                      │
│  DeepSeek API 设置                   │  ← 标题
│                                      │
│  ┌──────────────────────────────────┐│
│  │ API Key                    [显示] ││  ← OutlinedTextField
│  │ sk-••••••••••••••               ││
│  └──────────────────────────────────┘│
│                                      │
│  ┌──────────────────────────────────┐│
│  │           [ 保存 ]               ││  ← 全宽Button
│  └──────────────────────────────────┘│
│                                      │
│  ┌──────────────────────────────────┐│
│  │ 使用说明                         ││  ← surfaceVariant卡片
│  │                                  ││
│  │ 1. 前往 platform.deepseek.com    ││
│  │    获取 API Key                   ││
│  │ 2. 将 API Key 粘贴到上方输入框    ││
│  │ 3. 返回主页即可查看余额和用量     ││
│  │                                  ││
│  │ 模型说明：                       ││
│  │ • V4 Flash: deepseek-v4-flash    ││
│  │ • V4 Pro:   deepseek-v4-pro      ││
│  └──────────────────────────────────┘│
└──────────────────────────────────────┘
```

## 交互逻辑

1. **首次打开** → 显示欢迎提示 + "前往设置"按钮
2. **设置页输入API Key** → 保存到 DataStore
3. **返回主页** → 自动调用余额API → 与上次余额对比 → 记录差值到Room
4. **下拉刷新/点击🔄** → 重新拉取余额并更新
5. **柱状图** → 显示最近30天的每日token消耗

## 数据来源

| 数据项 | 来源 |
|--------|------|
| 余额 | DeepSeek API `/user/balance` |
| 今日消耗 | 本地Room数据库（当日记录之和）|
| 本月消耗 | 本地Room数据库（当月记录之和）|
| 模型tokens | 本地Room数据库（按model字段过滤）|
| 柱状图 | 本地Room数据库（最近30天按日聚合）|
